---
const stackHighlights = ['Astro', 'Tailwind', 'TypeScript', 'Dokploy', 'Docker', 'Cloudflare'];
const gitStats = [
  { label: 'Repos arquiteturais', value: '42' },
  { label: 'Commits 90d', value: '312' },
  { label: 'Followers', value: '540+' }
];
const learning = ['Temporal.io', 'WebAssembly runtimes', 'AI Observability', 'LLM Guardrails'];
---

<section class="space-y-6">
  <div class="flex items-center justify-between gap-4">
    <div>
      <p class="text-xs uppercase tracking-[0.5em] text-neutral-400">Layout Bento</p>
      <h2 class="text-2xl">Estado atual do estúdio arquitetural</h2>
    </div>
    <span class="rounded-full border border-white/10 px-4 py-1 text-xs text-neutral-300">Atualizado semanalmente</span>
  </div>

  <div class="grid gap-4 lg:grid-cols-6">
    <article class="rounded-bento border border-white/5 bg-surface-900/70 p-6 shadow-card lg:col-span-3">
      <p class="text-xs uppercase tracking-[0.3em] text-neutral-500">Stack Principal</p>
      <h3 class="mt-2 text-2xl text-neutral-50">Decisions over tools</h3>
      <p class="mt-3 text-sm text-neutral-300">
        Destaque para ferramentas que reforçam visão arquitetural e entregam governança.
      </p>
      <div class="mt-4 flex flex-wrap gap-2">
        {stackHighlights.map((item) => (
          <span class="rounded-full border border-white/10 bg-surface-950/70 px-3 py-1 text-xs font-medium text-neutral-100">{item}</span>
        ))}
      </div>
    </article>

    <article class="rounded-bento border border-white/5 bg-gradient-to-br from-primary-600/30 to-surface-900/60 p-6 shadow-card lg:col-span-2">
      <p class="text-xs uppercase tracking-[0.3em] text-neutral-200">Localização</p>
      <h3 class="text-xl text-neutral-50">São Paulo · Remote-first</h3>
      <p class="mt-2 text-sm text-neutral-200">Operação multi-fuso (-3 a +2). Workshops híbridos e roadshows executivos.</p>
      <div class="mt-4 rounded-2xl border border-white/10 bg-surface-950/60 p-4">
        <p class="text-xs text-neutral-400">Base atual</p>
        <p class="text-neutral-100">Latitude -23.55 · Cloudflare Warp always-on</p>
      </div>
    </article>

    <article class="rounded-bento border border-white/5 bg-surface-900/70 p-6 shadow-card lg:col-span-1">
      <p class="text-xs uppercase tracking-[0.3em] text-neutral-500">GitHub Stats</p>
      <ul class="mt-4 space-y-4">
        {gitStats.map(({ label, value }) => (
          <li>
            <p class="text-2xl font-mono text-primary-300">{value}</p>
            <p class="text-xs text-neutral-400">{label}</p>
          </li>
        ))}
      </ul>
    </article>

    <article class="rounded-bento border border-white/5 bg-surface-900/70 p-6 shadow-card lg:col-span-3">
      <p class="text-xs uppercase tracking-[0.3em] text-neutral-500">Currently Learning</p>
      <h3 class="mt-2 text-xl text-neutral-50">Explorando próximos pilares</h3>
      <div class="mt-4 grid gap-3 md:grid-cols-2">
        {learning.map((topic) => (
          <div class="rounded-2xl border border-white/10 bg-surface-950/70 px-4 py-3 text-sm text-neutral-100">
            <span class="mr-2 inline-block h-1.5 w-1.5 rounded-full bg-accent-500 align-middle"></span>
            {topic}
          </div>
        ))}
      </div>
    </article>
  </div>
</section>
